import { StyleSheet } from 'react-native';

export const loaderStyles = StyleSheet.create({
	container: {
		flex: 1,
		width: '100%',
		justifyContent: 'center',
		alignItems: 'center',
		paddingBottom: 24,
	},
});
